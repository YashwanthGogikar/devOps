pipeline {
  agent none
   stages {
    stage('Back-end'){
      agent {
        Docker {image 'maven:3.8.1-adoptopenjdk-11'}
      }
      steps{
        sh " java --version "
      }
    }
   }
   stages {
    stage('Front-end'){
      agent {
        Docker {image 'nodejs:latest'}
      }
      steps{
        sh "Node --version "
      }
    }
   }
}
